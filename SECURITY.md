# 安全策略 / Security Policy

## 漏洞反馈

如果发现疑似安全问题，请优先通过私密渠道反馈，不要直接创建公开 issue。

反馈时请尽量包含：

- 受影响的版本或 commit
- 部署方式
- 已移除密钥的请求样例
- 预期影响和实际影响
- 已移除私有文档内容的服务日志

## 关注范围

本服务中安全敏感的部分包括：

- 上传的 Markdown 和图片 payload 校验
- `API_KEY` 配置
- 限流
- DOCX 生成和 XML 处理
- 可选的浏览器图表渲染
- 字体文件处理

## 部署建议

- 本地桌面使用时，默认只绑定 `127.0.0.1`。
- 如果服务暴露到 localhost 之外，应设置 `API_KEY`。
- 共享或公网部署时，应放在 HTTPS 和带鉴权的反向代理之后。
- 不要记录私有文档内容或 API key。
